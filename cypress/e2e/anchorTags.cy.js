describe('Get href values of all anchor tags in mobile responsive view on Aluminate for education page', () => {
  it('Should log href values to the console in mobile view', () => {
    cy.anchortags({
    });
  });
});
